# 算法说明

## 几何校正

检测图先缩小，转灰度并双边滤波；Canny 阈值由中位亮度估计，闭运算连接边缘。为处理屏幕贴近照片边缘的情况，检测掩码允许沿图像边界闭合，同时排除几乎覆盖整幅图像的外框伪候选。轮廓经多边形逼近后只保留凸四边形，最终评分为面积、矩形程度、边缘强度、中心距离和对边平衡的加权和。若闭合轮廓完全缺失，检测器会以行列亮度跃迁提出一个保守的贴边矩形候选，并要求至少两条边得到 Canny 支持。对贴边候选，算法沿四条初始边的法线寻找梯度，以加权直线和迭代 MAD 排除内容边缘，再求直线交点；缺乏真实梯度的照片裁切边保持不动。失败时不会阻止手工四角。

AUTO 比例不再只取投影对边长度。给定原图尺寸时，算法假设零像素倾斜、主点在图像中心，由矩形横纵消失方向正交关系 `f² = -(vx-c)·(vy-c)` 估计焦距，再从 `K^-1 H` 前两列范数恢复物理宽高比；数值不稳定时才回退到边长。常见比例同时考虑横向、纵向和 A 系列纸张。固定 16:9/4:3 等模式会尊重四角的横竖方向。输出近似保持估计像素面积，再执行透视、裁边、旋转和黑边。

平面单应性只负责四角透视。镜头畸变在它之前校正，弯曲银幕在它之后通过 Mesh 校正；三种几何模型职责分离，避免用一个矩阵勉强拟合全部形变。滚动快门逐行形变仍不属于这三种静态模型。

## 镜头畸变标定

针孔模型使用 `k1/k2/k3` 径向项和 `p1/p2` 切向项；鱼眼模型使用四个径向系数。焦距分别相对于图像宽高归一化，主点位于 `[0,1]`，所以同一镜头在不同分辨率下可以复用参数。校正通过 `initUndistortRectifyMap` 生成输出到输入的逆映射，再以 Lanczos 重采样。

Web 标定要求同尺寸棋盘格照片，使用 3～30 个横纵内角点配置；只接纳检测到完整内角点的视角。至少 3 张才允许计算，界面默认要求 5 张不同姿态。结果返回总 RMS 和逐视角像素重投影误差；主点、焦距或畸变系数落入不可信范围时直接拒绝，不输出不稳定配置。

## 弯曲银幕 Mesh

透视恢复后的规则目标网格，与用户给出的归一化源控制点一一对应。两张控制点坐标场分别通过 `RectBivariateSpline` 插值成全分辨率逆映射，再使用 OpenCV `remap`。2×2 到 15×15 网格均可使用；每个网格单元会检查方向和退化，拒绝翻折。

对称起点用边界固定的三次弯曲函数生成，用户随后可拖动任意控制点。Mesh 能表达平滑弯曲银幕和轻度局部投影形变，但控制点本身仍需由画面直线、边界或人工判断提供。

## 多帧真实观测融合

多帧模块位于单图流水线之前。参考帧自动按可用曝光和 Laplacian 清晰度选择；代理图先用相位相关初始化，再用 ECC 求仿射/单应逆映射，失败时可回退到 ORB/RANSAC 或平移。低重叠、低对齐分数以及亮度内容相关性过低（例如电影画面已经切镜）的帧会被拒绝，避免把不同内容融合成重影。

全分辨率融合按条处理。每个像素权重综合有效覆盖、中间调可用性、饱和、低饱和高亮、相对时域中位数离群、局部 Laplacian 清晰度和对齐分数。曝光补偿只在双方低梯度中间调重叠区域估计对数增益，避免把内容或模糊造成的直方图变化误当曝光。

输出同时区分：参考帧不可靠但其他帧有可靠值的“真实观测补回”，以及所有帧都没有可靠值的“未解决”。后者不会进入生成式填充。多帧可解决反光位置变化、曝光包围、瞬态遮挡和至少一帧清晰时的局部模糊；所有帧同一区域都丢失时仍不可能物理还原。

## 条带校正

图像转 LAB 亮度，二维 Gaussian 先压制细节；分别求行均值与列均值。每条一维曲线通过 Gaussian 平滑分离慢趋势和周期残差，再用去直流频谱能量选择主方向。

影院/投影还支持宽光幕：每行低分位亮度近似黑位，大尺度趋势之外的正向抬升视为
散射/滚动快门光幕 `v(y)`，按加性模型 `T=(I-v)/(1-v)` 有限恢复。它只修正全宽
同步的黑位抬升，不能重建反光已经覆盖或饱和的内容。

校正增益近似为：

```text
gain = trend / max(trend + periodic, epsilon)
```

增益经平滑、均值归一化并限制到 `1 ± max_correction`，最后只修改 L 通道并按强度混合。慢渐变进入趋势项，因此不会被当作强条带；大面积与条带同方向的真实内容仍可能干扰估计。

合成测试同时叠加正弦条纹、逐行/列曝光偏差和慢渐变，并断言恢复结果对干净图的 MAE 确实下降。

## 去摩尔纹

色彩模式在 YCrCb 中对 Cr/Cb 做强于亮度的双边平滑。局部强度由热度图分配，热度综合色彩局部变化、亮度高频、全局周期峰和“彩色纹理与亮度边缘不一致”；字幕/文字边缘通过 Sobel 蒙版保护。

实验性频域模式对频谱中的显式点及其中心对称点应用柔和 Gaussian notch。用户可以在诊断窗口左键增加、右键删除陷波点。自动峰检测需要用户主动点击，流水线默认不自动删除高频。

细密真实纹理与摩尔纹在单帧频谱中可能不可分，过强处理会损失纹理。

## 白平衡与色调

- Gray World：将三个通道均值拉向共同均值。
- White Patch：使用各通道 99.2 百分位估计白点。
- 中性点：取点击位置周围 7×7 块均值。
- 所有增益均限制，避免近零通道导致极端色偏。
- Exposure 以 EV 的 `2^EV` 计算；Gamma 使用 `x^(1/gamma)`；高光、阴影、黑白点使用连续亮度权重。

## 局部对比度与照明

CLAHE 只处理 LAB L 通道并与原亮度混合。电影院预设使用低强度。

照明场可由大尺度 Gaussian 或 morphology opening 估计；乘法模型使用 `L × median(field) / max(field, epsilon)`，加法模型使用 `L + median(field) - field`。暗部保护降低近黑区域校正，避免噪声和黑位被过度抬升。

## 降噪、锐化与去模糊

降噪提供 Gaussian、bilateral、OpenCV 彩色 NLM 和 Y/Cr/Cb 分离处理。Unsharp Mask 使用 Gaussian 细节层、阈值蒙版和高光/暗部保护，不使用固定 3×3 核作为唯一方法。

实验性 Wiener 反卷积支持运动/高斯 PSF，以 `conj(H)/(abs(H)^2 + K)` 正则化。结果使用稳健分位数限制数值爆炸，再按强度混合。PSF 不匹配时会振铃，因此默认关闭。

## 反光

自动蒙版检测饱和高光及低饱和高亮区域；用户可画入/画出多边形并羽化。基础抑制只压缩局部高光。Telea/Navier-Stokes 仅在蒙版面积不超过 8% 时运行，避免把大面积反光误称为真实恢复。

实验性梯度 DCT 模式移植自 `fast-reflection-removal`：首层 Laplacian 删除低于阈值
的梯度，再对方程右端执行第二次 Laplacian，通过 DCT 对角化 Poisson/双调和算子求
解。项目增加稳健分位数配准和有限混合。软反射与真实低对比纹理不可完全区分，
因此该模式默认关闭。

## 可选学习模型

- NAFNet GoPro width32：同分辨率去模糊，实测对影院样例较自然但改善温和。
- Real-ESRGAN general-x4v3：支持强/弱降噪权重 DNI 与 1～4 倍输出，线条和头发
  细节提升明显，但可能生成原图不存在的纹理或绘画感。
- 两者均通过本地清单、外部进程和重叠 tile 运行，默认关闭，不参与经典结果，也不
  能修复错误几何、宽光幕或已经丢失的像素信息。
