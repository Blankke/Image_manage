# 算法说明

## 浮点与颜色空间

解码后保留只读 `uint8` 原图，进入流水线时一次转换为 `[0,1]` RGB `float32`。几何、
经典恢复、缓存、ONNX/OpenVINO 和模型融合均保持浮点，编码前才量化。sRGB 与线性光
使用 IEC 61966-2-1 分段函数互转；摄影 EV 和白平衡通道增益在线性光中计算，Gamma、
对比度等感知控件仍在 sRGB 中计算。这样可保留暗部的亚 1/255 中间值，并避免一次次
8 位往返产生阶梯和缓存误差。

## 几何校正

自动几何由统一服务编排，数据契约显式区分画芯/显示内容 `content_quad` 与物体/画框 `outer_quad`。正式 QuadLocator ONNX 输出四张 content 角点热图、四张 outer 角点热图、内容 mask、boundary、presence 和目标类别；网络负责选择正确语义层，不直接生成恢复像素。传统 Canny、轮廓、Hough 和行列跃迁候选保留为无模型诊断 fallback，但它们没有层级语义，单独不能支撑无人值守接受。

640 像素级粗角点投回原图后，每条边在受限窄带内沿法线采样 Sobel 响应，并可融合网络 boundary。候选点经加权总最小二乘和迭代 MAD 拟合四条直线，再以交点生成精修四角。精修只有在四边支持、凸性、图像范围和最大位移全部通过时才生效；否则保留粗角点。DocAligner FastViT-SA24 在当前四场景中只返回 2/4 个完整四边形，已检测样本平均 NCE 为 2.95%、IoU 为 0.8849，而且无 content/outer 与产品置信度。缺少语义 boundary 时，纯梯度精修还把均值变为 NCE 3.18%、IoU 0.8775，说明通用文档模型只能作为基线，精修 gate 仍需在真实标注集上校准。

接受策略联合 presence、类别、content 层置信度、几何合理性、边界支持和异常分布。任何条件不满足都返回结构化 `rejected` 及原因；Web、CLI、GUI 不得再各自实现一套候选选择或静默回退为全图。

AUTO 比例不再只取投影对边长度。给定原图尺寸时，算法假设零像素倾斜、主点在图像中心，由矩形横纵消失方向正交关系 `f² = -(vx-c)·(vy-c)` 估计焦距，再从 `K^-1 H` 前两列范数恢复物理宽高比；数值不稳定时才回退到边长。常见比例同时考虑横向、纵向和 A 系列纸张。固定 16:9/4:3 等模式会尊重四角的横竖方向。输出近似保持估计像素面积，再执行透视、裁边、旋转和黑边。

平面单应性只负责四角透视。镜头畸变在它之前校正，弯曲银幕在它之后通过 Mesh 校正；三种几何模型职责分离，避免用一个矩阵勉强拟合全部形变。滚动快门逐行形变仍不属于这三种静态模型。

## 镜头畸变标定

针孔模型使用 `k1/k2/k3` 径向项和 `p1/p2` 切向项；鱼眼模型使用四个径向系数。焦距分别相对于图像宽高归一化，主点位于 `[0,1]`，所以同一镜头在不同分辨率下可以复用参数。校正通过 `initUndistortRectifyMap` 生成输出到输入的逆映射，再以 Lanczos 重采样。

Web 标定要求同尺寸棋盘格照片，使用 3～30 个横纵内角点配置；只接纳检测到完整内角点的视角。至少 3 张才允许计算，界面默认要求 5 张不同姿态。结果返回总 RMS 和逐视角像素重投影误差；主点、焦距或畸变系数落入不可信范围时直接拒绝，不输出不稳定配置。

## 弯曲银幕 Mesh

透视恢复后的规则目标网格，与用户给出的归一化源控制点一一对应。两张控制点坐标场分别通过 `RectBivariateSpline` 插值成全分辨率逆映射，再使用 OpenCV `remap`。2×2 到 15×15 网格均可使用；每个网格单元会检查方向和退化，拒绝翻折。

对称起点用边界固定的三次弯曲函数生成，用户随后可拖动任意控制点。Mesh 能表达平滑弯曲银幕和轻度局部投影形变，但控制点本身仍需由画面直线、边界或人工判断提供。

## 多帧真实观测融合

多帧模块位于单图流水线之前。参考帧自动按可用曝光和 Laplacian 清晰度选择；代理图先用相位相关初始化，再用 ECC 求仿射/单应逆映射，失败时可回退到 ORB/RANSAC 或平移。低重叠、低对齐分数以及亮度内容相关性过低（例如电影画面已经切镜）的帧会被拒绝，避免把不同内容融合成重影。

全分辨率融合按条处理。每个像素权重综合有效覆盖、中间调可用性、饱和、低饱和高亮、相对时域中位数离群、局部 Laplacian 清晰度和对齐分数。曝光补偿只在双方低梯度中间调重叠区域估计对数增益，避免把内容或模糊造成的直方图变化误当曝光。

输出同时区分：参考帧不可靠但其他帧有可靠值的“真实观测补回”，以及所有帧都没有可靠值的“未解决”。后者不会进入生成式填充。多帧可解决反光位置变化、曝光包围、瞬态遮挡和至少一帧清晰时的局部模糊；所有帧同一区域都丢失时仍不可能物理还原。

## 条带校正

图像转 LAB 亮度，二维 Gaussian 先压制细节；分别求行均值与列均值。每条一维曲线通过 Gaussian 平滑分离慢趋势和周期残差，再用去直流频谱能量选择主方向。

影院/投影还支持宽光幕：每行低分位亮度近似黑位，大尺度趋势之外的正向抬升视为
散射/滚动快门光幕 `v(y)`，按加性模型 `T=(I-v)/(1-v)` 有限恢复。它只修正全宽
同步的黑位抬升，不能重建反光已经覆盖或饱和的内容。

校正增益近似为：

```text
gain = trend / max(trend + periodic, epsilon)
```

增益经平滑、均值归一化并限制到 `1 ± max_correction`，最后只修改 L 通道并按强度混合。慢渐变进入趋势项，因此不会被当作强条带；大面积与条带同方向的真实内容仍可能干扰估计。

合成测试同时叠加正弦条纹、逐行/列曝光偏差和慢渐变，并断言恢复结果对干净图的 MAE 确实下降。

## 去摩尔纹

色彩模式在 YCrCb 中对 Cr/Cb 做强于亮度的双边平滑。DISPLAY/LED 默认使用联合模式：
先从亮度高频构造局部结构张量，以特征值相干度判断纹理是否长时间保持同一方向，再以
局部高频能量调节处理权重。规则屏幕栅格会获得较高权重，多方向建筑、毛发和织物仅保留
受限的最低权重；另在更大尺度计算结构边缘，防止文字、字幕和轮廓被一起平滑。亮度与
Cr/Cb 分别执行双边滤波，色度使用较弱的相对混合，实际掩膜写入摩尔纹热图诊断。

实验性频域模式对频谱中的显式点及其中心对称点应用柔和 Gaussian notch。用户可以在诊断窗口左键增加、右键删除陷波点。自动峰检测需要用户主动点击，流水线默认不自动删除高频。

8 组配对实拍中，联合模式使平均亮度 SSIM 从旧 Fidelity 的 0.7773 提升到 0.8063；
摩尔纹较重的测试 4 和测试 6 相对 Geometry 分别提升 0.0980 和 0.0863。全局平滑虽能
获得更高平均分，但会肉眼软化测试 5，因此未采用。细密真实纹理与摩尔纹在单帧频谱中
仍可能不可分，过强处理会损失纹理。

## 高光扩散光晕

`dehalo` 不使用反锐化，也不移动真实边缘。它从亮度阈值提取高亮核心，用窄、宽两个
Gaussian 尺度之差估计只存在于核心外侧的正向散射层，并限制单像素最大扣除量。自动
门控要求画面属于暗场且高亮面积处于有限区间；明亮页面、天空和大面积白底不会触发。
诊断输出场景中值、高亮占比、是否触发及实际修正分位。数字原图本身已有的轮廓光不会
因“看起来像光晕”被删除；只有相机/屏幕额外产生的宽扩散层满足该模型时才处理。

## 白平衡与色调

- Gray World：将三个通道均值拉向共同均值。
- White Patch：使用各通道 99.2 百分位估计白点。
- 中性点：取点击位置周围 7×7 块均值。
- 所有增益均限制，避免近零通道导致极端色偏。
- Exposure 以线性光 EV 的 `2^EV` 计算；Gamma 使用 `x^(1/gamma)`；高光、阴影、黑白点使用连续亮度权重。
- 影院 Fidelity 只在低分位亮度低于激活上限时，把它解释为可能的抬黑证据；校正量
  受最大值限制，并仅在触发时追加 0.06 的轻微对比。明亮场景保持逐像素中性。该
  策略在“电影测试二”配对原图上同时改善 PSNR、SSIM、梯度相关和色差。
- DISPLAY Fidelity 的自动白场只在低饱和、亮背景占比至少 65% 时触发。它用该区域
  的亮度分位估计屏幕白点，在线性光中应用受限增益，并把证据面积、观测分位和实际
  增益写入诊断。7 组显示器配对图中只有白底“测试6”触发；其 Fidelity PSNR 从
  13.8780 提升到 17.6342 dB，ΔE 从 17.8458 降到 10.9669。

`restoration.photometric` 另提供 Archive 路径的受约束参数化校正：有限白平衡增益、
3×3 色彩矩阵、平滑照明增益场、全局曝光和单调 tone curve。输出仍由输入像素通过
确定性变换计算，不允许模型直接重画整张 RGB。后续轻量 residual 也同时限制空间混合
系数和单像素最大改变量。

## 局部对比度与照明

CLAHE 只处理 LAB L 通道并与原亮度混合。电影院 Fidelity 默认关闭；需要更强视觉
对比时由用户显式启用，避免把胶片颗粒和固有暗部扩张成伪细节。

照明场可由大尺度 Gaussian 或 morphology opening 估计；乘法模型使用 `L × median(field) / max(field, epsilon)`，加法模型使用 `L + median(field) - field`。暗部保护降低近黑区域校正，避免噪声和黑位被过度抬升。

## 降噪、锐化与去模糊

降噪提供 Gaussian、bilateral、OpenCV 彩色 NLM 和 Y/Cr/Cb 分离处理。Unsharp Mask 使用 Gaussian 细节层、阈值蒙版和高光/暗部保护，不使用固定 3×3 核作为唯一方法。

实验性 Wiener 反卷积支持运动/高斯 PSF，以 `conj(H)/(abs(H)^2 + K)` 正则化。结果使用稳健分位数限制数值爆炸，再按强度混合。PSF 不匹配时会振铃，因此默认关闭。

## 反光

自动蒙版检测饱和高光及低饱和高亮区域；用户可画入/画出多边形并羽化。基础抑制只压缩局部高光。Telea/Navier-Stokes 仅在蒙版面积不超过 8% 时运行，避免把大面积反光误称为真实恢复。

实验性梯度 DCT 模式移植自 `fast-reflection-removal`：首层 Laplacian 删除低于阈值
的梯度，再对方程右端执行第二次 Laplacian，通过 DCT 对角化 Poisson/双调和算子求
解。项目增加稳健分位数配准和有限混合。软反射与真实低对比纹理不可完全区分，
因此该模式默认关闭。

## 可选学习模型

- 模型清单必须声明 `role` 和 `task`。`restoration` 节点位于经典伪影处理之后，用于
  去模糊、去噪、去摩尔纹等观测恢复；`enhancement` 节点位于末端并允许生成统计纹理。
- NAFNet GoPro width32：同分辨率去模糊；在本项目合成屏幕筛选中对干净内容漂移
  明显，未进入默认 Fidelity。
- Real-ESRGAN general-x4v3：支持强/弱降噪权重 DNI 与 1～4 倍输出，线条和头发
  细节提升明显，但可能生成原图不存在的纹理或绘画感，只能出现在 AI Enhanced。
- 两者均通过本地清单、外部进程和 float32 重叠 tile 运行，默认关闭，也不能修复
  错误几何、宽光幕或已经丢失的像素信息。1× Real-ESRGAN 会在每个 tile 内尽早把
  固定 4× 网络输出缩回目标倍率，避免分配整幅 4× 浮点累加缓冲。

## 评估协议

`oracle_restoration` 的 8 组实拍/数字原图采用 SIFT 特征、Lowe ratio 与 MAGSAC 单应性定位屏摄内容。数字原图参与求取实拍四角，因此它只衡量准确几何已知时的恢复能力，不能宣称为自动定位或端到端成绩。输出同时报告直接缩放与二次小量
配准后的 PSNR、亮度 SSIM、梯度相关、CIELAB 色差、锐度比例、频谱峰偏差及黑白裁切。
`scripts/evaluate_paired.py` 还生成 Geometry、Fidelity、AI Enhanced、差异热图和
电影五阶段消融图与指标表，防止只凭单张锐度或主观观感调参。

`e2e_auto` 在定位结果冻结前只读取手机照片，之后才读取人工角点或 clean reference
打分。报告同时统计拒绝覆盖率、高置信度正确率、错选层级、NCE 分位数和 Quad IoU
分位数。少于 100 个独立 group 的集合只能标记为 smoke，不能通过发布 gate。
